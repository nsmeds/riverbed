{{outlet}}

<div id="main-content">
    {{#each model as |post|}}
        {{#post-item post=post}}
            {{!--{{post.author.name}}--}}
            {{post.content}}
        {{/post-item}}
    {{/each}}
</div>
